platform: node

tasks:
    build:
        command: 'pnpm run build'
        inputs:
            - 'package.json'
            - 'pnpm-lock.yaml'
            - 'README.md'
            - 'tsconfig.json'
            - 'src/**/*'
        outputs:
            - 'dist'
